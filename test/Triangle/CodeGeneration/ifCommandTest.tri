let
	proc P() ~ 
		loop for t ~ 0 to 10 do
			putint(t);
			puteol()
		repeat
	end
	;
	proc Q() ~
		loop for t ~ 0 to 20 do
			putint(t);
			puteol()
		repeat
	end;
	var x init 10
in
	if x >= 10 then
		P()
	else
		Q()
	end
end